# lefthook.yml
pre-push:
  jobs:
    - name: lint
      run: npm run lint

    - name: test
      run: npm test

pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.js"
      run: npm run lint

    - name: test
      glob: "__tests__/**/*.js"
      run: npm test
