# lefthook.yml
pre-push:
  jobs:
    - name: lint
      run: npm run lint

    - name: test
      run: npm test

pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: "cd web && npm run lint"

    - name: test
      run: "cd web && npm test"
